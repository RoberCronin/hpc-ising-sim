This is a placeholder readme. Useful commands:

```
pip install "./libising[dev]"
pip install --no-build-isolation -ve libising
pip install --no-build-isolation -Ceditable.rebuild=true -ve libising
```

